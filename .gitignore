node_modules/
dist/
.next/
out/
build/
coverage/
.turbo/
*.tsbuildinfo

# env / secrets
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# uploaded file blobs (metadata is in DB). Anchored so it does NOT match the
# source dir apps/api/src/common/storage/. Covers both local run locations: repo
# root (`node apps/api/dist/main.js` from root) and apps/api/ (dev runs from there).
/storage/
/apps/api/storage/
