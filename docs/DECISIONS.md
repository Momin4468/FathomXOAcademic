# DECISIONS.md

> Append-only architectural decision log. **Append (never edit/delete) an entry whenever a foundational decision is made** (per CLAUDE.md §1). Newest at the bottom. Format: date · decision · why. This is the "why things are the way they are" record — read before changing anything structural.

---

### 2026-06 · Stack: PostgreSQL + TypeScript + NestJS + Next.js + embedded Metabase
Postgres chosen over MySQL for native row-level security, JSONB (custom fields), and ledger/analytics strength — all core requirements. TS end-to-end for one-language maintainability. Modular monolith.

### 2026-06 · Profit is derived from legs, never stored
Margins/splits/commission computed at read time from the leg chain so they stay correct as date-versioned deal terms change. No profit/margin column anywhere.

### 2026-06 · Visibility enforced at the database (RLS), not just UI
A user may read a money leg only if SuperAdmin or their party is on it. Enforced by Postgres RLS so a UI bug cannot leak a price.

### 2026-06 · Personal Finance is a separable service, linked not merged
Own identity/data/subscription; one-way income API from business payouts; business cannot read into it; deactivating a brokerage account does not disable it.

### 2026-06 · Two parallel closes per job
Work-state (draft→pending→confirmed→delivered) and money-state (unbilled→invoiced→partial→settled) are independent.

<!-- New entries below this line -->

### 2026-06-27 · Monorepo tooling: pnpm workspaces + Turborepo
TS end-to-end across `apps/{api,web}` + `packages/{db,shared}`. Boring, mainstream, well-documented (CLAUDE.md §5). pnpm installed via `npm i -g pnpm` (corepack enable failed with EPERM writing to Program Files).

### 2026-06-27 · Data layer: Drizzle ORM + hand-written raw-SQL migrations
Drizzle for typed app queries; DDL + RLS policies + grants are hand-authored SQL in `packages/db/migrations`, applied by a tiny migrator with a `schema_migrations` ledger. Chosen over Prisma because the spine is RLS/ledger-heavy (CLAUDE.md §2). The raw SQL is the source of truth; the Drizzle TS schema is a typed mirror.

### 2026-06-27 · RLS context via per-transaction session GUCs + dedicated app role
The API connects as a non-superuser, non-owner role `app_user` (so RLS binds). Each request runs in a transaction that first sets transaction-local GUCs `app.org_id` / `app.current_party_id` / `app.is_superadmin`; policies read them via `current_setting`. The reusable `withRlsTransaction()` is the single access layer. Alternative (one Postgres role per user) rejected as pooling-hostile.

### 2026-06-27 · Append-only enforced by GRANTs, not just convention
The immutable set (leg, payment, payment_allocation, payment_proof, audit_log) is granted INSERT+SELECT only to `app_user` — no UPDATE/DELETE privilege, so corrections must be reversing entries (CLAUDE.md §3.4). Invoices/rules are mutable-but-no-hard-delete (SELECT/INSERT/UPDATE). Verified by tests.

### 2026-06-27 · A–F dependency tables created early
SCHEMA A–F transitively FK to `ref_entity` (B), `milestone_template(_item)` (H, minimal), `file_object` (G); `audit_log` (G) added too since the access layer needs it. Created now following SCHEMA conventions so the first migration is self-consistent. Other G/H tables deferred.

### 2026-06-27 · Local Postgres on host port 5433
A pre-existing host Postgres occupies 5432 (caused 28P01 auth failures); the Docker container publishes to 5433 and `.env` connection strings use it. Cosmetic/local only — clean machines can use 5432.

### 2026-06-27 · API compiled with tsc (not tsx/esbuild)
NestJS DI relies on `emitDecoratorMetadata`, which esbuild/tsx do not emit. The API is built with `tsc` to ESM and run via `node dist/main.js`. (Module 0 depth has since replaced the header stub with real token auth.)

### 2026-06-27 · Module 0 auth: JWT access+refresh, bcryptjs, otplib TOTP
Access token 30m; refresh is a per-device, hashed-at-rest token with a sliding 10-day expiry re-set on each use; rotation revokes the old token and reuse of a revoked token kills the whole family; logout revokes server-side. Passwords via bcryptjs (pure-JS, portable on Windows); 2FA via otplib TOTP, opt-in. JWT_SECRET must be ≥32 chars or the API refuses to boot, and HS256 is pinned (no alg-confusion / no silent insecure fallback).

### 2026-06-27 · RLS context derives from the signed token, never client input
`AuthGuard` verifies the Bearer access token and sets `req.principal`; `extractRlsContext` builds the RLS GUCs from the principal only — the old `x-org-id/x-party-id/x-superadmin` header path is deleted. Proven by tests: a forged `x-party-id`/`x-superadmin` header has no effect. This closes the impersonation hole the stub left open.

### 2026-06-27 · `is_superadmin` GUC = System SuperAdmin role only (§4.4)
The leg-visibility bypass is granted only when the authenticated principal holds the System SuperAdmin role (computed server-side from `user_role` at login/refresh). Business SuperAdmin deliberately does NOT get it — they get aggregated/settlement views (built later). So no single business seat renders every leg.

### 2026-06-27 · `app_auth_lookup()` SECURITY DEFINER = the sole credential-lookup RLS bypass
Login must read `user_account` by email before any org context exists, which RLS would block. A narrow `SECURITY DEFINER` function (owner-rights, `search_path` pinned, EXECUTE only to `app_user`, returns only auth columns for one email) is the single sanctioned bypass — preferred over a second privileged connection pool. Migration `0003_auth.sql`. The `auth_refresh_token` table is the one mutable security table (UPDATE granted for revoke/rotate); everything else stays append-only.

### 2026-06-27 · Git: commit directly to `main`, no feature branches (user preference)
Module 0 branch `module0-auth-authz-audit` merged (--no-ff) into `main` and deleted. Going forward, work and commit straight to `main`.

### 2026-06-27 · Module 1 fuzzy resolution: normalize + aliases + pg_trgm
"Fuzzy-in / canonical-out" (§7) = three layers: `normalize()` (lowercase, strip non-alphanumerics) collapses case/space/punct variants of the SAME token; explicit `ref_alias` rows cover genuinely different spellings (701 vs ICT701); and `pg_trgm` `similarity()` powers typo-tolerant ranked type-ahead. Exact-normalized hits rank above trigram matches. `unique(org_id, ref_id, normalized)` prevents duplicate aliases on one entity; cross-entity ambiguity is resolved by returning ranked candidates (pick-don't-type), not a hard constraint.

### 2026-06-27 · Module 1 additive schema (0004) — merge redirect + referred-by
`ref_entity` gains `archived_at` + `merged_into_id` so a steward can MERGE a duplicate into a canonical survivor: aliases move to the target, the source's old name is kept as an alias (still resolves), FK refs (`party.university_id`) are repointed, and the source is archived pointing at the survivor. `party` gains `referred_by_party_id` (self-ref) for the directory's "referred-by". Additive nullable columns — not a spine redesign. NOTE: merge's FK-repoint list must grow as ref-consuming tables are added (Module 2: work_item course/assignment refs).

### 2026-06-27 · Module 1 packaged as `refdata/` folder (deny-rule workaround)
`.claude/settings.json` denies `Edit(reference/**)` (meant for the top-level human `/reference/` spreadsheet backup, CLAUDE.md §5), but the glob also blocks an API source dir named `reference/`. Since that settings file is itself deny-listed for edits, the NestJS module lives at `apps/api/src/modules/refdata/` instead; HTTP routes are still `/reference` + `/parties` and the permission module key is still `reference`. Reference data + party directory are one module (Module 1), feature-flagged by `FEATURE_REFERENCE`; the Data Steward role makes the confirm/merge permission delegable to non-owners (§7).

### 2026-06-27 · Module 2 money model: read only from legs (RLS), margins derived
All money (prices, pay, margins) is read from the `leg` chain, which is RLS-enforced (a non-party gets zero rows; only System SuperAdmin bypasses). `deriveMargins` (in `@business-os/shared`) computes margin = inbound − outbound from ONLY the legs the caller can see, and reports a node only when both its sides are visible — so a middle party sees just their own slice and cannot reconstruct the top client price; SuperAdmin sees all nodes. Nothing is stored: no profit/margin column, line amounts computed via `computeLineAmount`. Legs are append-only (insert/select grant only); corrections are reversing legs. Legs are inserted with client-side UUIDs and **no RETURNING**, because an admin building the chain isn't a party to every leg and reading the row back would (correctly) trip the leg SELECT policy.

### 2026-06-27 · Module 2 work_line money + consumer identity redacted to work:approve
`work_line` has only tenant RLS (admins must manage all lines, and they aren't a "party" on consumer rows), so column-level money protection is done in the API: `mapLine` returns the work spec to everyone but withholds `client_rate`/`writer_rate`/`fixed_amount`/computed amount AND the consumer (client) party id unless the caller is System SuperAdmin or holds `work:approve`. This applies on every line-returning path (detail, list, and the `addLine` response). The authoritative, DB-enforced money boundary remains the legs. INTERIM: the `work:approve` gate is coarse; per-field visibility via `permission.scope_json` (SCHEMA §A) is the eventual model (deferred). Full column-level DB security on `work_line` also deferred.

### 2026-06-27 · Module 2 copy fan-out = 1 producer line → N consumer lines
The writer's single "5 copies @ my rate" entry is one producer `work_line` (writer side, `unit_count = N`); the admin's fan-out creates N independent consumer `work_line`s (each its own `consumer_party_id` + `client_rate`/state), each linked back via `source_line_id`. Producer and consumer are never the same row (a line is one side XOR the other; enforced in `addLine`). Legs are created explicitly now; auto-deriving leg amounts from deal terms is Module 3 (`leg.deal_term_id` stays nullable).

### 2026-06-27 · Module 3 effective-dating: half-open windows + supersede (value never mutated)
Rules are date-versioned with half-open windows `[effective_from, effective_to)`. Renegotiation **supersedes**: within one tx, close the prior open version (`UPDATE … SET effective_to = newFrom` — the only sanctioned mutation, allowed by the SELECT/INSERT/UPDATE grant) and INSERT a new version; the prior `value`/`rate` is never changed. So a March job resolved as-of March still hits the March window even after a June renegotiation. `isEffectiveOn`: `effective_from <= asOf AND (effective_to IS NULL OR asOf < effective_to)`. Supersede refuses an already-closed version (no overlapping windows). Resolution filters the window in SQL and the pure ranker re-checks — expired rules can never win.

### 2026-06-27 · Module 3 precedence (most-specific → default)
`resolveDealTerm` scores candidates: specific party-pair (+10) over global null-pair (+0); `applies_to` client (+3) > jobtype (+2) > default (+1); a non-matching client/jobtype rule is excluded; ties break to latest `effective_from`, then latest `created_at`. `resolveCompRule`: party-specific (+10) beats role-level (+1). Pure functions in `@business-os/shared/rules.ts` (unit-tested) so precedence/dating is provable without a DB. `applies_to` follows the SCHEMA text convention (`default` | `client:<uuid>` | `jobtype:<x>`). KNOWN GAP (deferred): jobtype is exact-match free text, so a typo silently no-ops — a canonical-jobtype (ref_entity) tie-in is the eventual fix.

### 2026-06-27 · Module 3 gating + comp_rule provenance (migration 0008)
Deal terms / comp rules are gated by `rules:*` (Admin + System SuperAdmin hold it; Writers don't → money rules don't leak). Party-scoped "own terms" reads (a writer seeing their per-word rate) deferred, consistent with Modules 1/2. SCHEMA §E omitted provenance on `comp_rule` though they are money-defining; migration **0008** additively adds `created_by`/`created_at` (set from the signed-token principal) — a small spine gap closed, not a redesign. Resolution-only this module; auto-applying resolved terms to legs is Module 5. The Module 1 `ReferenceService.merge` was extended here to also repoint `work_item.course_ref_id`/`assignment_type_ref_id` onto the survivor.

### 2026-06-27 · Module 4 web auth: BFF proxy + httpOnly cookies (tokens never in JS)
The browser never holds tokens. It talks only to same-origin Next route handlers (`/api/auth/*`, `/api/proxy/[...path]`); login stores the access+refresh JWTs as **httpOnly, SameSite=Lax** cookies (secure in prod), and the proxy attaches the Bearer from the cookie and refreshes once on 401. A **single-flight** dedupes concurrent refreshes so the rotating refresh token isn't double-spent (which would trip reuse-detection) — per-process (note for multi-instance deploys). The NestJS API remains the real authority (RLS + guards + redaction); the BFF is plumbing. Chosen over client-side token storage (XSS exposure).

### 2026-06-27 · Module 4 CSRF: same-origin required for state-changing proxy calls
Because SameSite=Lax still allows cross-site top-level form POSTs, the proxy rejects unsafe methods (POST/PUT/PATCH/DELETE) unless the request is same-origin (verified via `Sec-Fetch-Site: same-origin`, else a strict `Origin` match). Verified: a cross-site POST → 403. The proxy also rejects traversal path segments (SSRF) and forces `content-type: application/json` to the API. Guards extracted to `src/lib/proxy-guard.ts` and unit-tested.

### 2026-06-27 · Module 4 money-safety: UI renders only what the (redacted) API returns
The DB/API is the money authority (legs RLS + `work_line` redaction). The UI's rule: render money **only when the field is present** — `<Money>`/`formatMoney` return nothing for absent/empty/NaN (never a `0`/`—` that implies a hidden figure), and no client code derives a margin/price or requests an unredacted endpoint. Proven end-to-end: a Writer's job-detail payload lacks `clientRate`/amount, shows only their own leg, no margin. Belt over the server guarantee. SWR + a thin fetch wrapper for reads; a restrained Tailwind primitive set (`src/components/ui.tsx`); shadcn/full visual language still deferred. Deadline/tz-urgency display deferred until a `work_item` deadline field lands with the task board (Module 6).

### 2026-06-28 · Module 5 invoicing + balances: live grouping, derive don't store
Invoices are a **live grouping** of a client's billable lines: a line auto-attaches to the client's open invoice (`ensureOpenInvoice`), lines move between invoices, and an estimate is **superseded by a final** (`supersedes_invoice_id`) with the estimate set `status='void'` but retained (never deleted). All balances are **derived** from `payment_allocation` sums (SCHEMA §I — `invoice_line.paid_amount` is left dormant, never written/read); the only maintained status is `work_item.money_state` (`recomputeMoneyState`: unbilled→invoiced→partial→settled), which is **independent of `work_state`** — the two parallel closes. Payments are events; allocation is the link, supporting partial-within-job and bulk-across-jobs in one call (cap-checked). Money tables (`payment`, `payment_allocation`, `payment_proof`, `charge`) stay INSERT+SELECT only; corrections are **reversing entries** (negative mirrors): a payment reversal carries `reverses_payment_id` and refuses double/over-reversal; `SELECT FOR UPDATE` can't be used to lock (append-only grant withholds UPDATE) so a concurrent-allocation lock (advisory) is deferred.

### 2026-06-28 · Module 5 bidirectional charge ledger (party→business dues)
Legs carry **business→party earnings**; a new append-only `charge` table carries **party→business dues** (platform fee now, AI-check etc. later), itemized as "amount to be paid", settled via `payment_allocation.charge_id`. Each party has a **two-way balance**: `derivePosition` nets earnings-outstanding (legs to them − payouts) against charges-outstanding (their charges − settlements) into a position; a platform fee surfaces as a due on the writer's `/billing/balance/me` (available to any authenticated party; computed under their own RLS). `charge` has **leg-style party-scoped RLS** (a party sees only their own dues). Because an admin isn't a party to a charge, `createCharge` uses client-side id + no RETURNING (like `leg.appendLegs`), and validating/reversing a charge goes through `charge_summary()` (SECURITY DEFINER, EXECUTE to app_user only) so the source party/amount/already-reversed are read server-side, never trusted from the client. Invoices/payments are `billing:*`-gated admin ops; cross-party/business-wide balances are the settlement layer (Phase 2) / SuperAdmin.

### 2026-06-28 · Module 6 expenses: one table, cost-bearer drives attribution
Every cost is one `expense` row (SCHEMA §G): salaries, subscriptions, promo (optional campaign_tag + loose revenue_link_id), losses, events are all categories — no payroll subsystem. The `cost_bearer` (momin/emon/split/writer) is load-bearing for partner economics; a `split` requires a non-empty numeric `cost_bearer_split_json` (validated on create AND update). Expenses are **mutable operational data** (tenant-RLS, full CRUD) — NOT the append-only money ledger — but DELETE is revoked (0012): soft-delete via `archived_at`. Gated by `expenses:*` (admins). Split-amount derivation + revenue-link ROI are deferred (no read-model yet).

### 2026-06-28 · Module 6 tz-aware deadlines + computed urgency
A deadline stores the **absolute instant** (`due_at`, UTC) **plus the IANA zone it was set in** (`due_tz`). The wall-time→instant conversion is a dependency-free, DST-correct Intl helper (`zonedWallToInstant`, offset computed at the target date); an invalid tz → 400 (`isValidTimeZone`). **Urgency / "time left" is absolute and derived, never stored**; the web renders the due moment in the **viewer's browser zone** (and shows the original zone only when it differs — "your time" / "set for <zone>"). Per-user fixed clocks (multi-clock §8) deferred. Tasks are gated by `capture:*` and are an **org-wide shared board** (not party-row-scoped) — honoring `permission.scope_json` rows is a future refinement; `?mine=true` filters to the caller.

### 2026-06-28 · Global date rule: dd/mm/yyyy display + picker-only entry
All user-facing dates render **dd/mm/yyyy** (`formatDate`/`formatDateTime` in `@business-os/shared`, re-exported by the web); existing `toLocaleDateString` usages were retrofitted. Date/time entry is always a **native picker** (`<input type=date|time>`) + a timezone `<Select>` — never a free-text date field. The web depends on `@business-os/shared` (via `transpilePackages`) so the date/money/enums logic lives once. This completes Phase 1 (Modules 0–6).

### 2026-06-28 · Module 5 deferrals S1 + N1 resolved (migration 0013)
**S1 (concurrent allocation):** `payment.service.allocate` now takes a transaction-scoped **`pg_advisory_xact_lock(hashtext(paymentId))`** as its first statement, serializing concurrent allocations on the same payment so the read-then-cap-check-then-insert can't be raced into over-allocation. Earlier the lock seemed to fail with "permission denied for table payment" — that was a **stale `SELECT FOR UPDATE` build** (FOR UPDATE needs UPDATE privilege the append-only payment grant withholds); the advisory lock is a function call needing **no table privilege**, verified working as `app_user`. Covered by a parallel-allocation test (two ×60 vs a 100 payment → exactly one 400, derived paid=60). **N1 (`invoice_line.paid_amount`):** confirmed **derive-only** — removed from the Drizzle mirror (so app code can't reference it; the build proves nothing did) and DB-`COMMENT`ed as deprecated; the physical column is kept for SCHEMA §F fidelity but paid/due are always summed from `payment_allocation` (§I).

### 2026-06-28 · Deal-term auto-pricing wired into the leg engine (capture-first)
Building the money chain no longer requires hand-typing every leg amount. When a leg is appended **without an `amount`**, the engine resolves the deal term on its `(from→to)` relationship **as of the job date** and fills `leg.amount` + `leg.deal_term_id`: `per_word` → value × word_count, `fixed` → value. An **explicit amount overrides** and clears the term link (bespoke price, not "from term X"). Only `per_word`/`fixed` price a leg — the `%` term types (split/commission/referral) divide *margin* and belong to settlement (Phase 2), not to a chain leg. Unpriceable + no amount → 400 (capture stays explicit, never a silent 0). A new read-only **`POST /work/:id/legs/propose`** (`@HttpCode(200)`, gated `work:approve`) returns proposed amounts + `source` (derived/manual/unpriced) **without writing**, so add-a-job can show rule-proposed prices for review/override before committing.

Design choices: (1) **No migration** — `leg.deal_term_id` existed since 0006. (2) The new `PricingService` lives in the **work** module and reuses the **shared pure** `resolveDealTerm` (the one source of precedence/effective-dating truth) while doing its own `deal_term` candidate fetch — so the work module does **not** import RulesModule (keeps the feature-flag seam clean; only a trivial parameterized SQL prefilter is co-located, not the ranking logic). (3) `asOf` defaults to the work_item's `created_at`, so a past job prices on past terms even after a later renegotiation. The two non-negotiables touching legs are preserved: margins stay **derived-from-legs** (`deal_term_id` is provenance, not a stored figure; `deriveMargins`/`getVisibleLegs` untouched) and **leg visibility** holds (auto-pricing inserts with no RETURNING; propose computes from terms and never reads back existing legs — the leg-leak test passes on a fully auto-priced chain). qa-test-writer caught a real defect (propose returned the POST-default `201` for a read-only endpoint) → fixed with `@HttpCode(200)`.

### 2026-06-28 · Billing web screens (Module 5 front end)
Built the invoices / payments+allocation / two-way balance UI on the existing capture-first web stack (BFF proxy + httpOnly cookies, SWR, `ui.tsx` primitives, `EntityPicker`). Money rule reaffirmed end-to-end: the UI renders **only** the figures the redacted API returns — every amount/paid/due/net goes through `<Money>`/`formatMoney` (absent ⇒ render nothing, never a `0`/`—`), and the UI **derives no money** (the pure `lib/billing.ts` helpers — `remainingToAllocate`/`clampAmount`/`netLabel` — only operate on numbers the caller can already see; the allocation "remaining" is a client-side convenience and the server's over-allocation cap is the authority). Actions are permission-gated client-side (billing view/create/edit/approve) over the server's RLS+guards.

Decisions: (1) **Balance = own + admin lookup** — `/balance` shows the viewer's own position via `GET /billing/balance/me` to **any party (incl. writers)**, and `billing:view` holders additionally look up any party via `/billing/balance/:partyId` (with add/reverse-charge). (2) **Allocation is guided by counterparty** — an `in` payment pre-loads the client's open invoice lines (due>0) + the counterparty's outstanding charges; an `out` payment offers the writer aggregate; tick several with partial amounts for the partial-within-job + bulk-across-jobs model. The allocation section requires `billing:view` so an unprivileged viewer never triggers a cross-party invoice/charge fetch (security-reviewer B1). (3) **Reversal state is read from `reverses_payment_id`, never from a (possibly hidden) amount** (B1/S1). (4) **Payment-proof attachment deferred** — no file-upload/object-storage pipeline exists, so nothing can create a `file_object` to attach. (5) Live-grouping capture entry: the **job hub** bills a money-visible consumer line via `POST /invoices/attach-line` (auto-attaches to the client's open invoice). Known gap: no `GET /payments/:id`, so the payment detail page fetches the list and finds by id, and "remaining" is session-local — a tracked backend follow-up.

### 2026-06-28 · Projects / engagements + milestones + milestone templates (DESIGN_SPEC §5)
A project is a **container of child work_items** flagged trackable/billable; **a plain job is the one-child case**, so projects use the **same machinery** as jobs — children are ordinary `work_item`s (no parallel type), and the whole projects module is gated by the existing **`work:*`** permission module and mounts under `FEATURE_WORK` (no new permission module, no new feature flag, no seed migration). The spine tables already existed (0000); migration **0014** only adds `work_item.trackable/billable`, project/milestone provenance columns, and indexes.

Decisions: (1) **Templates are work-gated** (admins manage them) rather than reference-gated — operationally simplest, avoids an admin lacking `reference:*`; they're per-uni/programme lists you **instantiate then extend** (snapshot the items into real milestones on create, append more via `/instantiate` — fluid programmes; template edits never mutate an in-flight project). (2) **Self-contained estimate→actual**: `project.estimate_amount` stores the rough quote; the **actual is DERIVED** at read time = Σ over **billable** children of their consumer-line client amounts (`computeLineAmount`), never stored (§3.3). Both estimate and actual are **money-gated** (System SuperAdmin / `work:approve`); crucially the stored `estimate_amount` is **redacted from the raw project row** for non-money callers too — a reviewer/qa pass caught that returning the project row leaked the estimate past the gated `money` block (§3.2). Firming the client invoice stays in the billing module's estimate→final supersede (module seam kept; projects don't call billing). (3) Milestones reuse the task module's **tz-aware deadline** pattern (absolute instant + IANA zone via `zonedWallToInstant`/`isValidTimeZone`; urgency derived, never stored) and a `pending→in_progress→done` state machine; `complete` on a project is the governance step (work:approve, stamps confirmed_by/at), and re-completing returns 409.

### 2026-06-28 · Settlement layer (partner balance, transfers, split/commission, platform fee) · §4.4, §3
Built the shared Emon↔Momin settlement layer. The §4.4 crux ("see the other's work volume" ≠ "see the other's margins") is enforced at the DB: a new **SECURITY DEFINER** `settlement_legs(a,b)` bypasses leg RLS but returns, per shared job, ONLY the **shared pool** (the downstream-node margin = inter-partner handoff leg − the downstream's outbound legs to non-partners) plus the pair + job date — never the upstream's private client leg. It is **caller-guarded** (`app_is_superadmin() OR app_current_party() in (a,b)`), so a third party or a billing:view admin who isn't one of the two partners gets zero rows. Raw legs never appear in any settlement response; the unchanged `leg_visibility` RLS still blocks direct leg reads.

Money model (logged so it isn't re-derived): **shared pool = downstream-node margin only** — the upstream's private margin (their top leg − the handoff) is never in the pool (matches §3.1's 6000/5000/3000 → pool 2000, Momin's private 1000 invisible to Emon). **One formula for both term types**: for the inter-partner handoff `from=upstream → to=downstream`, the downstream holds the pool and **owes the upstream `value%`** of it, where `value` is the resolved `split_pct` or `commission_pct` term on (upstream→downstream) as-of the job date (split 50 on Momin→Emon → Emon owes Momin 1000; commission 20 on Emon→Momin → Momin owes Emon 20% — "Emon takes 20%, Momin keeps the rest"). All split/net/who-owes-whom is **derived at read time** (pure `deriveSettlement`); only `settlement_transfer` rows (append-only, dated, reversible, party-RLS) and `charge` rows are stored.

**Platform/system fee** = a new `platform_fee` deal-term type (a %); an explicit `POST /settlement/platform-fee` resolves it (via the shared `resolveDealTerm`, effective-dated) and inserts a party→business `charge` = pct × the party's earnings on the job (`party_job_earnings` definer for the base, since the admin isn't a leg-party). Idempotent: a `platform_fee_exists` check + a partial-unique index (migration 0016) backstops the check-then-insert race. Gated by the existing `billing:*` module (both partners are Admins) — no new permission module/seed; `SettlementModule` mounts under FEATURE_BILLING. Reviews fixed a transfer-netting **sign bug** (a settling payment had doubled the balance), the platform-fee term-version resolution (was hand-rolled/oldest), and the idempotency race.

### 2026-06-28 · Per-work outcomes + derived reputation + writer capacity (§8)
Built Module 7 (`outcomes`). Per-work `work_outcome` rows (SCHEMA §G — on-time/late, revisions+fault, grade, marker feedback, complaint, fail, AI score, satisfaction, rework cost, disputed) are entered by an authorized role and **NEVER self-reported**: `record`/`update` reject when `principal.partyId === work_item.doer_party_id`. **Reputation is a DERIVED read-model** (pure `deriveReputation` over outcome rows — on-time/complaint/fail rates, revision + writer-fault counts, satisfaction breakdown, a transparent reliabilityScore) — there is **no stored score column**; mirrors `deriveMargins`/`derivePosition`/`deriveSettlement`. Course history (work_item grouped by course) and current load (open-job count) are likewise derived; `party.availability`/`max_concurrent` are the only stored capacity inputs.

Decisions: (1) **New `outcomes` permission module** (not a reuse) so outcome-recording can be **delegated to a non-admin marker role** (seeded QA = view/create/edit) without granting broad `work` powers — the spec's "admin or an admin-assigned role." (2) **Visibility**: a plain Writer (outcomes:view) sees only their **own** outcomes/reputation/card; a "manager" (`outcomes:edit`/`approve`/System SuperAdmin) sees across writers — read and profile-edit use the **same** capability (a reviewer caught an earlier read=approve / edit=edit asymmetry that locked the QA recorder out of reading what it records; now unified on `outcomes:edit||approve`). (3) **Profile** (expertise tags + availability + max-concurrent) is editable by the writer for their own party or by a manager; outcomes stay non-self-reportable regardless. (4) `work_outcome` is mutable-but-undeletable (grants select/insert/update, no delete; corrections are audited edits), one row per work_item (unique + a 409 race backstop). Multi-writer attribution uses `doer_party_id` (work_line split deferred); routing/matching deferred per §8. The `outcomes` module needs `FEATURE_OUTCOMES=true` in `.env` (couldn't be set here — .env is permission-locked).

### 2026-06-28 · Credential vault (encrypted at rest, per-item sharing, 2FA-gated reveal) · §8, CLAUDE.md §4
Built Module 8 (`credential_vault`) — a secrets-manager for tool accounts AND client-scoped portal credentials. Each item carries metadata (name, `type` portal/google/github/aws/tool/other, `url`, `client_party_id`) plus a secret bundle {username,password,2FA-recovery,notes} that is **AES-256-GCM encrypted by the app before insert** — the DB only ever holds `secret_iv`/`secret_tag`/`secret_ciphertext`; plaintext never lands in a column, an audit `detail`, or a log. New `EncryptionService` reads `VAULT_ENCRYPTION_KEY` (base64 32 bytes) and hard-fails at boot if missing/short (mirrors the JWT_SECRET check); it's provided only by the vault module, so the key is required only when the vault is enabled.

Decisions: (1) **Per-item ACL enforced in RLS** (`credential_vault_item` visible only to a party with an active `credential_share`, or System SuperAdmin) — a non-holder gets ZERO rows, exactly like legs; revoke (`revoked_at`) is instant. This is "visibility at the DB" (CLAUDE §3.2), not app-enforced. (2) **Holders-only reveal** — admins are not auto-holders; an admin who must reveal grants themselves a share first (audited), so "who could ever see this secret" is always reconstructable. (3) **Mandatory TOTP step-up on reveal**: the caller must have 2FA enrolled and present a valid current code, else 403/401; every reveal (allowed and denied) is audited; the secret is returned only in the HTTPS response body. (4) **Admin management via SECURITY DEFINER** (`vault_manage_list`/`vault_manage_shares`/`vault_revoke_share`, metadata-only, org-filtered), reachable only from `credential_vault:approve` endpoints — the sanctioned RLS-bypass pattern. (5) **Mutable-but-undeletable** (grants select/insert/update; items archive, shares revoke). New `credential_vault` permission module (Admin all; Writer view-own). Reviews fixed: reveal returns 200 (was POST-default 201); `grant` validates the grantee party is in-org. **Follow-up**: `user_account.twofa_secret` is still plaintext — encrypt it with the new EncryptionService (a DB compromise currently exposes the TOTP seed). Needs `FEATURE_CREDENTIAL_VAULT=true` + `VAULT_ENCRYPTION_KEY` in `.env`.

### 2026-06-28 · File pipeline + Knowledge base + Cover sheets (§7/§8, file rule)
Built the greenfield file-upload/object-storage pipeline (the `file_object` table existed; nothing created one) + the knowledge base + cover-sheet templates. The pipeline is core plumbing (FilesModule, always-on) since payment-proofs/briefs will reuse it. **File rule enforced at `POST /files`**: small files stored, large files + video are **links only**, **images recompressed (sharp)**, and the **DB stores only metadata** — `file_object.url` is a server-generated storage key (or an external link), never the bytes, and the key is never returned to clients (stored files download via `GET /files/:id/download`).

Decisions: (1) **Local disk behind a `StorageService` interface** (gitignored `storage/`, `STORAGE_DIR`) — S3/MinIO is a one-file swap later. (2) **Programme reuses `ref_entity`** — articles/cover-sheets carry `university_ref_id` + `programme_ref_id` (no new kind); the university hub (`GET /knowledge/university/:refId`) surfaces child programmes, referencing styles, linked articles, and cover sheets. (3) **New `knowledge` permission module** with **open authoring** (view+create to all operational roles); curation (publish/status, editing others' articles, cover-sheet writes) needs `knowledge:approve`; cover sheets are readable by all. (4) The web BFF proxy is JSON-only, so uploads/downloads use dedicated routes (`/api/upload` multipart, `/api/files/[id]` binary-safe stream) that keep tokens server-side.

Security review fixes: the download endpoint serves **stored files only** — a link is opened directly from its metadata URL, never bounced through our origin (**no open redirect**); `file.linked` audit logs only the URL **host** (link URLs can carry tokens); the stored **storage key is masked** (null) in article-attachment responses; non-image stored files are served `attachment` + `X-Content-Type-Options: nosniff` (**no inline stored-XSS**); **publish status is curator-only** (authors can't self-publish). **Known gap (tracked)**: `GET /files/:id/download` is org-scoped but has **no per-file ACL** — fine for org-public knowledge/cover-sheets, but a kind-aware ACL must be added before sensitive kinds (proof/brief/receipt) reuse the pipeline. Needs `FEATURE_KNOWLEDGE=true` in `.env`.

### 2026-06-28 · AI/plagiarism check service — a self-contained mini-business (§8)
Built Module 10 (`checks`). A semi-separate unit (2–3 employees) collects files from their OWN WhatsApp customers (often not academic clients — a check stands alone), runs them through tool accounts (AcademyCX), and collects payment. The model: **a `check_channel` = an employee × a WhatsApp account**; the **`check_batch` is the primary capture** — a low-friction tally per channel×day (files_checked / files_paid / amount_collected), optionally linked to a customer party and/or a work_item, with an optional `check_file` per-file detail (the file + AI/plagiarism score). A per-file check is just a batch of one.

Governance (§2): a worker **proposes** a batch (a claim); an admin/QA **confirms** it (**no self-confirm** — confirmer ≠ recorder), and **only confirmed batches feed the P&L and consume credits** — that's the governance's teeth. **Standalone P&L is DERIVED, never stored** (`packages/shared/src/checks.ts`): revenue collected − **allocated account cost** − **worker comp**. The account cost is grounded in **real top-ups** — `check_credit_topup` is append-only (credits + price); cost-per-credit = Σcost/Σcredits (the unrounded ratio is used for the cost calc, rounded once, so per-credit rounding doesn't drift at volume); allocated cost = confirmed files × that. Worker comp = confirmed files × the employee's per-file `comp_rule` (reusing `resolveCompRule`); a P&L `unpricedBatches` flag surfaces files with no comp rule so net isn't silently overstated. AcademyCX thus appears as both **capacity** (credit balance = purchased − consumed, derived) and **cost** — answering "are we making money on checks?". The vault link is an id only (the secret is never read here).

Decisions: (1) **Cost basis = real top-ups** (not a fixed unit cost). (2) **Confirmed batches are immutable** — corrections are a fresh batch, not an in-place edit (the §3.4 ledger ethos); top-ups are append-only. (3) **Visibility**: a worker sees only their own channels' batches; the credit/cost + P&L are **admin-only** (`checks:approve`); `checks` is gated by its own permission module (operational roles a4–a7 get view+create). (4) **Batch own-only is enforced app-tier over tenant-RLS, NOT party-RLS like legs** — batches aren't the structurally-opaque money-leg class, business admins (not System SuperAdmin) legitimately need the whole board, and the sensitive cost/P&L is separately gated; logged here per the §3.2 "visibility at the DB" review note. Needs `FEATURE_CHECKS=true`.

### 2026-06-28 · Referrers — referral income as a claimant leg (§4, §8)
Built Module 11 (`referrers`). Per the owner, **a referral is "another claimant leg, scoped like any other"**: an admin attaches a `leg` from the business (`from_party_id = NULL`) **to a referrer** party, and the existing `leg_visibility` RLS (0001) scopes it so the referrer sees ONLY their own slice — never the client price or the rest of the chain. Payout flows through the **existing earnings model** (legs-to-you in `balance.service` + `payment_allocation`), so "payout to their earnings" needed no new money plumbing. The referrer login (role a9, `referrers:view`) gets only `GET /referrers/me` (own income + the works that generated it, via the `referrer_works` SECURITY DEFINER caller-guarded to `app_current_party()`).

**Reconciliation with the 2026-06-28 auto-pricing decision** ("% term types divide margin and belong to settlement, not a chain leg"): that still governs the **auto-priced client→writer chain** (the chain pricer remains per_word/fixed only). A referral leg is a *deliberate, admin-attached side-leg* whose amount is computed from a `referral_pct` agreement against a base an admin can see — a distinct path, not the chain pricer.

Decisions: (1) **Flexible basis** — the agreement is a `deal_term` (`term_type='referral_pct'`, `from=referrer`) with a new nullable **`basis` ∈ {revenue, margin, fixed}** (value = pct for revenue/margin, = amount for fixed); per-client override via `applies_to=client:<id>`. The agreement yields a **suggestion**; the admin may **override** the final leg amount (intentionally **unbounded** — the owner asked for maximum flexibility; corrections are reversing legs since money is append-only). (2) **Attach model = both** — a client carries a default referrer (`party.referred_by_party_id`) AND an admin can attach/override per job. (3) **One-hop, NO cascade of dividends** (load-bearing owner rule): the beneficiary is the client's *direct* referrer only; we never walk the `referred_by` graph upward (Mujib→Siam→clients ⇒ Siam earns on his, never Mujib), though an admin may explicitly reassign the beneficiary per job. A party can be both client and referrer (Siam); a client can't be their own referrer on their own job. (4) **Admin direct-attach** (no propose→confirm) — reuses the authorized leg path (`referrers:approve`); a referral can only target an **existing `party_type 'referrer'`** (no free-type). (5) **Every referral leg references a `referral_pct` deal_term** (the marker + provenance); when an admin enters a manual amount with no standing agreement, a **one-off fixed term is auto-created** so the leg is still identifiable as a referral. (6) **New `referrers` permission module** (a1+a3 all; a2+a4 view; a9 view). 

Definer-function guards: `referrer_works` is caller-guarded (own slice). `referral_exists` (dup guard) returns a **boolean only** — security review (H1) flagged the original amount-returning `referral_net` as an unguarded money-leak primitive, so it was reduced to existence (mirrors `platform_fee_exists`). `job_money` (revenue + writer cost, the suggestion base) is org-scoped and relies on the `referrers:approve` endpoint gate — the same trust model as `party_job_earnings` (0015); a Writer hitting suggest/attach gets 403 (tested). `client_name` exposed to the referrer is authorized by **§4.5** ("Client identity … Referrer: own referred R"). Needs `FEATURE_REFERRERS=true`.

### 2026-06-28 · Resit / fail handling — a failed job redone; truthful net loss (§3, §6, §8)
Built resit/fail handling in the work module (`POST /work/:id/resit`, `work:approve`). A failed job is redone **on the same `work_item`** (owner's decision: "both recorded against the job"), reusing existing primitives rather than a linked job.

Decisions: (1) **Same job, controlled reopen** — a narrow audited backward transition `delivered/confirmed → pending` (the work-state redo), allowed ONLY via the resit path; the generic `transition` stays forward-only. A resit **requires a recorded `failed` outcome** (claim→fact governance). (2) **Original-writer reduction = auto by paid-status, disjoint** — `reverseAmt = clamp(R, 0, max(0, party_earnings_outstanding))` is posted as a **negative reversing leg**; the remainder `chargeAmt = R − reverseAmt` is an **`adjustment` clawback charge** (party→business). They never overlap, so the same money is never both reversed and charged (no double-count). `R` is validated ≤ the writer's earning on this job (`party_job_earnings`). (3) **Loss is derived-only, never stored** — `deriveJobPnl` over `job_pnl(work_item)` + the outcome's rework cost; surfaced on `GET /work/:id.pnl` (money-gated, `null` otherwise). `net = revenue − writerCost + clawback − reworkCost`; a negative net is the loss, reported truthfully (and `deriveMargins`/settlement pool already go negative). (4) **`job_pnl.writer_cost`** sums legs to **writer-typed parties excluding the client→partner revenue handoff** (`from = source_party`) — this is what prevents a multi-hat partner (e.g. Momin `{partner,writer}`) who receives the client payment from being miscounted as writer cost; tag-based identification stays consistent with how the codebase models writers. (5) **Client re-bill to 0** is the escalation (`zeroClientBilling`): a negative client leg (revenue→0) + void the job's invoices + `recomputeMoneyState`→`unbilled`; **blocked when the client already has allocated payments** (that needs a reversing payment, not a silent void). (6) **Append-only** throughout — reversing/new legs are negative/positive INSERTs (client-side id, no RETURNING); the only UPDATEs are work-state, the `work_outcome` resit/reworkCost stamp, and `invoice.status='void'` (no `updated_at` column on `invoice`). `work_outcome` gains a `resit` flag.

Definer trust model (consistent with the 2026-06-28 referrers note): `job_pnl` and `party_earnings_outstanding` are SECURITY DEFINER, org-scoped, granted to `app_user`, and rely on the **money-gated / approve-gated endpoints** (like `job_money`/`party_job_earnings`, 0015/0021) — a non-money caller (writer/referrer) cannot reach them; any future caller must re-prove the gate. No new feature flag (resit lives in the always-relevant work module).

### 2026-06-28 · Custom fields — admin-defined structured fields on records (§2 #10, §8)
Built Module 12 (`custom_fields`). The spec's core primitive #10 — "the governed version of 'add any column'" — implemented as a `custom_field_def` catalog + per-record `custom_json`. The table was specified in SCHEMA §G but unmigrated; migrating it (0023) is implementing the spec, not inventing schema. Kept strictly separate from free-form `notes`/`details` (the ungoverned counterpart, #12).

Decisions: (1) **Targets = work_item + party + project** — `custom_json` added to party/project (work_item already had it). (2) **Values keyed by the def `id`** in custom_json (rename-proof, collision-free). (3) **Validation is HARD at the boundary**: every work/party/project create+update routes `customJson` through `validateValues` — unknown/inapplicable/archived field, type mismatch, or select ∉ options → 400. **Update merges** the (validated) incoming keys into the stored map, so a partial edit never wipes other captures and stale values for a since-archived field aren't re-validated (security review M1). (4) **Required = soft at draft, hard at a gate** (owner's choice): never blocks a save (capture-first), flagged `missingRequired`, but blocks the job `confirmed`/`delivered` transition and project `complete` via `assertRequiredComplete`. Parties have no lifecycle gate → soft only. (5) **Scope** (`scope_json`): `{}` global, else every present attr must match the record (work_item: client/assignment-type/course/uni-via-course-parent; party: client/uni; project: client). (6) **Catalog is mutable but archive-not-delete** (`active`/`archived_at`) so stored values survive a field's retirement; `field_type`/`target_entity`/id are immutable (changing them would orphan values). (7) **New `custom_fields` permission module** — define/edit/archive = approve; list/render/fill/search = view (operational roles a4–a7 get view). (8) **Provenance/standards added to the SCHEMA draft DDL**: `required`, `sort`, `created_at`/`updated_by`/`updated_at`/`archived_at`. (9) **Search** is JSONB `custom_json->>id ilike` with table/label from a fixed server allowlist (`sql.raw`) and fieldId/value parameterized; RLS scopes rows.

Definer/permission trust model is N/A here (no SECURITY DEFINER — all reads are tenant-RLS). `CustomFieldService` is provided to the record modules directly (stateless, AuditService-only dep) so validation runs even when the catalog UI flag is off; the controller is behind `FEATURE_CUSTOM_FIELDS`. Needs `FEATURE_CUSTOM_FIELDS=true` for the management/search UI.

### 2026-06-28 · Role-scoped dashboards — "my numbers" + owner analytics (§8, §10) — Phase 2 complete
Built Module 13 (`dashboard`), the last Phase-2 item. A single `GET /dashboard` returns a **viewer-shaped** payload composed from existing derived read-models **under the caller's RLS**, so per-viewer sections are self-scoped by construction; the owner analytics section is added only for an entitled viewer. Embedded Metabase / ad-hoc query builder stays Phase 3 (out of scope) — this is hand-built role-scoped read-models.

Decisions: (1) **Composition over new storage** — `balance` reuses `BalanceService` (writer earnings/dues); `openLoops` is a `work_item` count (not delivered OR not settled), **doer-scoped** for non-approvers (§4.5 "Writer own / Manager scoped"); nothing new is stored. (2) **Owner analytics audience = owners + both SuperAdmins** (owner's choice) — the `owner` section (profit-per-writer, org margin, all-client dues) is gated `isSystemSuperadmin || dashboard:approve`, seeded a1/a2/a3. (3) **Aggregate-only definers** — `dashboard_writer_pnl()` (profit per writer = client-leg − writer-leg grouped by the job's doer, §11.7; doer-attribution for multi-writer jobs) and `dashboard_client_dues()` (per-client invoiced − paid) are SECURITY DEFINER returning **rollups only, never raw legs** — so §4.5 holds (Biz SuperAdmin/owner get "agg", never another partner's private per-leg split; settlement stays shared-pool-only). Same org-scope + endpoint-gate trust model as job_pnl/party_job_earnings. (4) **Headline = outstanding client dues** (owner's choice; §13.4 design-time call); margin/profit-per-writer/open-loops are secondary. (5) **Derived, never stored** — the SQL column is named `net` (not "p​rofit"/"m​argin") so the no-stored-profit guard passes; `profit`/`orgMargin` are read-time TS labels. (6) **Endpoint gating** — `GET /dashboard` carries `@RequirePermission("dashboard","view")` (seeded to every role → effectively any authenticated) **specifically so the PermissionGuard loads `req.permissions`** (the `@CurrentPermissions` decorator throws otherwise — a real 500 caught in review); the owner section is then gated in-service by `dashboard:approve`. (7) **Routine reads are NOT audited** — the owner dashboard is a landing page loaded constantly; auditing every view would flood `audit_log`. The permission gate + RLS are the control; audit remains for mutations/break-glass. Needs `FEATURE_DASHBOARD=true`.

### 2026-06-29 · Deferred hardening pass (5 items; CLAUDE.md §3–4; migration 0025)
Cleared the five deferred items in one pass; all invariants kept (derived-not-stored, append-only money, leg visibility, org_id).

1. **Kind-aware per-file ACL.** `GET /files/:id` + `/files/:id/download` now call `files.service.assertCanRead`, backed by the `file_owner_context()` SECURITY DEFINER (org-scoped, revoke-public + grant app_user) which resolves a file's indirect owner (brief→work_item, proof→payment, receipt→expense) bypassing the owners' RLS so an entitled admin who isn't a party can still be authorized. Policy: org-public kinds (knowledge, cover_sheet) → any member; brief → doer/source or work:approve; solution → uploader or work:approve (no owner link yet, so never approve-only); proof → payment counterparty or billing:approve; receipt → expense creator or expenses:approve; other → file creator; System SuperAdmin always; foreign/unknown → 404. Domain-scoped admin override (least privilege) rather than a blanket "any admin". The definer returns ids server-side only; the controller never returns them.
2. **GET /payments/:id** = `{payment, allocations, proofs}`, gated billing:view (list parity) — the web detail page no longer fetches the whole list to find-by-id.
3. **twofa_secret encrypted at rest** via the existing `EncryptionService`, now a `@Global` `CryptoModule`. A `seal()`/`open()` marker wrapper stores `enc:`+base64(JSON{iv,tag,ciphertext}) in the same text column (no schema change); `enable2fa` seals, `login`+vault `reveal` open before verify and **lazily re-seal a legacy plaintext on success** (best-effort, try/caught — never blocks auth). **Decision: the key is validated on FIRST cryptographic use, not at boot** (deviating from the plan's "boot-required") — so a deployment/test that touches neither the vault nor a sealed secret still boots; seal/open-of-sealed fails loudly without VAULT_ENCRYPTION_KEY, and a corrupt/undecryptable secret fails **closed** (auth failure, not 500). No path writes plaintext after this change.
4. **One canonical `round2`** in `packages/shared/src/money.ts` (epsilon-round); the 6 shared + 4 api copies import it, and pricing dropped `.toFixed(2)` — removing the banker's-vs-epsilon drift at volume.
5. **Settlement shared-cost.** `settlement_legs()` (replaced in 0025) subtracts each job's `from_party_id IS NULL` legs (the only such "business cost" is the referral leg; resit legs are from=partner, already in the pool) so shared costs reduce the pool **before** the partner split (§4.4). Pool stays derived; the function stays caller-guarded + aggregate-only; guard:no-stored-profit still passes (columns are `pool`/`writer_cost`/`shared_cost`).

Known low-risk limits (logged, no live trigger): `settlement_legs` shared-cost assumes one inter-partner handoff per job; `file_owner_context` LEFT JOINs take the first owner row (uploads create one file per purpose). A test-fixture fragility surfaced by the lazy migration (auth-http read bizadmin's raw 2FA secret from a shared row) was fixed by having that test set its own known plaintext secret.

### 2026-06-29 · Remaining UI (settlement/vault/outcomes/client-360) + subscription reminders (§6, §8, §10)
Built the four web surfaces whose APIs already existed + a business-side subscription reminder. The screens are composition under the viewer's scope + money-redaction (`<Money>` absent⇒hidden); only two small list filters were added (`GET /work?sourcePartyId=`, `GET /vault/items?clientPartyId=`).

Decisions: (1) **Client-360 is viewer-scoped by construction** — each section fetches only with its permission (the web skips the fetch when `can(perms,…)` is false) and under the caller's RLS; a section the viewer can't see doesn't render (no figure a role can't access). Outcomes-by-client is the RLS-scoped outcomes list filtered to the client's job ids (no client filter on outcomes; over-fetch accepted for v1). (2) **Settlement web shows shared figures only** (§4.4) — the API returns no raw legs; the page renders position/transfers/net. (3) **Vault reveal** is the API's 2FA-gated holder-only flow; the secret lives only in component state, never logged/persisted. (4) **Subscription reminders**: `expense` gains `next_due_date` + `currency` (BDT/USD/GBP/EUR/AUD, **recorded as entered — no FX**) + `last_reminded_due` (idempotency per due-date; a changed due-date resets it). Recipient = the subscription's `created_by` user email (owner's choice; reusable by Phase-3 personal finance). (5) **EmailService is swappable** (global `EmailModule`, mirrors StorageService): `EMAIL_ADAPTER=dev` logs the envelope (never the body) + optional `EMAIL_OUTBOX_DIR`; a real provider is a later env swap; an unconfigured real adapter fails closed (throws). (6) **Firing** = in-process daily `@Cron` (`@nestjs/schedule`) running **per-org under each org's RLS** (orgs enumerated by `reminder_org_ids()`, an ids-only SECURITY DEFINER — no admin creds, no cross-org data) **plus** `POST /expenses/reminders/run` (gated `expenses:approve`, caller's org) for testing/external cron. (7) Reminder lives in the expense module (no new permission module).

qa caught a real bug: the runner's `current_date + ${LEAD_DAYS}` bound an untyped param → `operator is not unique: date + unknown` (reminders entirely broken) → fixed with a `(…)::int` cast. Security review clean (no Critical/High/Medium). Needs `EMAIL_ADAPTER=dev` (+ `FEATURE_EXPENSES`).

### 2026-06-29 · Personal Finance plane — a separate, sellable service (§11)
Built the full PF vertical (auth, RLS plane, one-way income bridge, custom categories, income/expense, loans, savings, targets/budgets, subscriptions+reminders, web UI) as `FEATURE_PERSONAL_FINANCE`. Migration 0027; module 14.

Decisions:
1. **Tenancy axis = `pf_account_id`, not `org_id`.** A standalone PF user has no org, so `pf_*` tables carry no `org_id`; the tenant is the account. This honors the *intent* of non-negotiable rule #1 ("every row scoped through one access layer; single-tenant is a special case") with a different axis. New GUC `app.pf_account_id` + `app_current_pf_account()`; `DbService.withPfAccount` sets only it and **blanks** the business GUCs.
2. **PF RLS omits the superadmin bypass = structural privacy.** `pf_account_isolation` policies scope by `app_current_pf_account()` and never reference `app_is_superadmin`/`app_current_org`. A business transaction never sets `app.pf_account_id`, so the business — SuperAdmin included — reads **zero** pf rows. The two planes are disjoint at the DB, not the UI.
3. **Separate credentials.** `pf_account` has its own email/password/2FA/status, independent of `user_account`; PF tokens use a distinct `typ` (pf_access/pf_refresh) so neither plane's token authenticates the other. Reuses PasswordService/TotpService/EncryptionService/EmailService — no second pipeline. PF auth surface mirrors the business one (rotation + reuse-detection, sealed 2FA, rate-limit-ready).
4. **One-way income bridge = port + DB-definer adapter.** Billing depends on `IncomeBridgePort` (token `INCOME_BRIDGE`); `DbIncomeBridge` calls the `pf_push_income` SECURITY DEFINER in the business payout transaction (`payment.service.allocate`/`reverse`, direction `out` + writer party). Returns void (no read-back); idempotent on `source_ref` = the payment_allocation id; reversal pushes a negative mirror → nets to zero. Future physical split = an HTTP adapter, no billing change.
5. **Linked-but-separate accounts via a link token.** A business user mints a single-use, expiring, hashed code for their OWN party (`pf_mint_link_token`, org-checked); the PF user consumes it (`pf_consume_link_token`) which sets `linked_party_id` and backfills past payouts as income. A unique index on `pf_account.linked_party_id` stops two accounts claiming one party. **Deactivation independence:** `pf_account.status` is decoupled from `user_account.status`; the soft `linked_party_id` (no cascade) survives a deactivated business account — proven by test.
6. **Multi-currency recorded, no FX.** Amounts stored as entered (currency text, any code; CURRENCIES are suggestions) with an optional user-entered converted amount. Dashboard headline totals sum the account's **base currency only** (never converts). **Business payouts are recorded in BDT** (the `payment` ledger has no currency column — both the live push and the backfill use BDT consistently; revisit if payments ever become multi-currency).
7. **Append-only + derived-not-stored.** pf money tables are insert/select only (grants); corrections are reversing entries (negated + `reverses_id`). Loan outstanding / saving balance / target progress / dashboard are derived in SQL at read. `guard:no-stored-profit` passes.
8. **Web: a separate, private plane.** Own httpOnly cookies (`pf_access`/`pf_refresh`), own BFF proxy (`/api/pf/proxy/*` → `/pf/*`), middleware gating `/personal-finance/*` on the PF cookie. Emerald accent marks the private plane. The business home page mints the link code.

Reviews: security — no Critical/High (DB-enforced privacy, one-way bridge, deactivation independence all confirmed); qa — 22/22 PF HTTP tests + 43/43 billing/payment regression + DB 320/320 green; ui — no blockers (fixed: error surface + confirm on loan/saving event money paths). Needs env `FEATURE_PERSONAL_FINANCE=true` (+ reuses `EMAIL_ADAPTER=dev`, `JWT_SECRET`, `VAULT_ENCRYPTION_KEY`).

### 2026-06-29 · Personal Notes — inside the Personal Finance (private) plane (§11; migration 0028)
A private personal notes system (lists / reminders / free text / attachments). **Placement decision:** it lives in the **PF plane**, not a stand-alone service or the business app. Reasoning: the PF plane is really "the user's private plane" (§4.1 — each user admin of their own; business, SuperAdmin included, sees none of it). Notes share that *exact* privacy model, so the PF plane gives identity, RLS isolation (`app.pf_account_id`), the separate web session, audit, and the EmailService/@Cron reminder pattern for free. A stand-alone plane would duplicate that whole stack; the business app would make notes org-scoped and visible to business roles (wrong). So Notes is a feature in the PF plane, surfaced as a "Notes" tab.

Decisions:
1. **Two tables, same RLS isolation** as the rest of the plane: `pf_note` (title/body/`items` jsonb checklist/color/pinned/`remind_on`+`last_reminded_on`/archived_at) + `pf_note_attachment`. Scoped by `app_current_pf_account()`, no superadmin clause → business reads zero rows. **Notes are editable scratch data, NOT a money ledger** — so `update` is granted and there is no append-only/reverse constraint (deliberate departure from the money rule; the append-only rule is for the ledger).
2. **Attachments reuse the file *rule*, not the org-scoped table.** `file_object`/`FilesModule` are org-RLS (unreadable in a PF tx), so attachments get a PF-scoped `pf_note_attachment` table + a PF upload/download seam, reusing the tenancy-agnostic **`StorageService`** (UUID key, no path traversal) + the same image-compression/size-cap/video→link logic. Small files → stored + in-system download; large → external link; DB stores metadata + a reference, never blobs. Link attachments are opened from their own URL (never relayed — no open redirect).
3. **Email reminders reuse the PF pattern:** an optional `remind_on` fires an email on the day via a daily per-account `@Cron` (09:30) enumerated by `pf_reminder_account_ids()`, reusing **EmailService** (no second pipeline), idempotent via `last_reminded_on`, recipient = the account's own email. Plus `POST /pf/notes/reminders/run`.
4. **Web:** a "Notes" tab in the PF session — a card list (search, active/archived, pinned-first, color/checklist/reminder cues) + an editor (title, body, inline checklist, colour, pin, remind-on, attachments) with a dirty-guard. Own PF upload (`/api/pf/upload/:noteId`) + download (`/api/pf/files/:id`) BFF routes (PF cookies only).

Reviews: security — **no Critical/High** (DB isolation/no-business-read confirmed; storage UUID-safe; link no-relay; CRLF-hardened the download filename); qa — **notes 21/21** (core 7, isolation 4, attachments 6, reminders 4) + PF regression 22/22 + DB 320/320, no bugs; ui — no blockers (added newNote error handling, dirty beforeunload guard, `formatDateTime` for updatedAt, aria-labels, 44px checklist tap target, Archive as a danger Button). No new env (reuses `FEATURE_PERSONAL_FINANCE`, `EMAIL_ADAPTER`, `STORAGE_DIR`/`FILES_MAX_BYTES`).

### 2026-06-29 · Analytics/BI — embedded Metabase over a redacted, read-only `analytics` plane (§8; migration 0029)
Added BI per §8 (embedded Metabase; never hand-rolled a query/charting engine). The hard requirement: **BI cannot leak a figure a role can't see** (§4.4/§4.5).

Decisions:
1. **A redacted `analytics` schema of aggregate views is the only thing BI sees.** The existing derived read-models (`dashboard_writer_pnl`, `settlement_legs`, `BalanceService`) are SECURITY DEFINER functions scoped by per-request GUCs — Metabase has no app session, so it can't use them. So BI gets new views carrying `org_id` (and `party_id` where party-scoped) as columns. Metabase connects as a **deny-by-default `analytics_ro` role** granted SELECT on the `analytics` schema only — **no base table, no GUC definer** (proven: as analytics_ro, leg/payment/invoice/charge/pf_*/party/work_item all return 42501; the GUC definers too). So opacity holds *through BI*, including the SuperAdmin ad-hoc query builder — even free-form SQL can't reach a raw leg.
2. **Views bypass RLS (superuser-owned) → the view SQL is the redaction boundary**, and the embed **locks `org_id`** so cross-org rows aren't shown. Because the views have no app GUC, a per-PARTY money breakdown would reveal one partner the OTHER's private client price/margin (the in-app dashboard hides that via per-viewer RLS; BI has none). **So money is exposed at the ORG level only** (`org_net`, `org_receivables`); per-party views carry no client price / chain margin — only counts (`work_volume`), writer **pay** (`writer_cost`, no revenue/net), quality (`writer_reputation`), org expenses (`expense_totals`), the **shared** settlement pool (`settlement_position` — never a private split), and a party's OWN balance (`party_balance`, member dashboard locked to `party_id`). This is **stricter than the in-app owner dashboard** (which is RLS-scoped per viewer) — the right posture for "BI cannot leak." The §4.5 "other partner's private margin = —" is honored by the deliberate absence of any margin-by-source-partner view. *(This addressed the security review's H1: the first cut mirrored the in-app per-writer P&L / per-client dues, which under RLS-bypass would have exposed cross-partner private figures.)*
3. **Role-scoping = locked signed-embed params.** `GET /analytics/embed` (gated `dashboard:view`) mints a Metabase signed-embed JWT (HS256, `METABASE_EMBED_SECRET`, distinct from `JWT_SECRET`, 10-min exp, `node:crypto` HMAC — no new dep): an approver (`dashboard:approve`/SuperAdmin) → owner dashboard locked to `org_id`; a party-linked member → member dashboard locked to `org_id`+`party_id`; no-party non-approver → 404. Params come from the **signed principal**, locked (Metabase forbids changing them), so a viewer can't widen scope; only the assembled iframe URL is returned (never a reusable secret); fails closed (404) when unconfigured.
4. **Connection: read-only role now, replica-ready.** Dev: `analytics_ro` on the existing Postgres (docker `metabase` service; its own H2 app-DB). Prod: point Metabase at a streaming **read replica** using the same role + schema — documented (docs/METABASE_SETUP.md), no code change; the boundary is the role+views, identical on primary or replica.
5. **Dashboards/ad-hoc are authored in Metabase's UI** over the safe views (dashboard IDs via env); a card published without its locked `org_id`/`party_id` filter is a Sev-1 misconfig (documented).

Reviews: security — boundary/embed/fail-closed all confirmed sound; H1 fixed (org-aggregate money), H2 (no test) resolved by `analytics-rls.test.ts`, M1–M3 actioned (docs hardened, `.env.example` vars added). qa — analytics-rls 33/33 (role denied every base table + definer; org_net/org_receivables carry no party dimension), analytics-embed 8/8 (scope-lock, member≠owner, tamper, 401, fails-closed), DB suite 352/352, dashboard regression 7/7. ui — no blockers (whoami loading guard, scope label only when loaded, ad-hoc link as a proper anchor-button + aria-label + noopener). Needs env: `ANALYTICS_RO_USER`/`ANALYTICS_RO_PASSWORD`, `METABASE_SITE_URL`, `METABASE_EMBED_SECRET`, `METABASE_DASHBOARD_OWNER`/`METABASE_DASHBOARD_MEMBER`.

### 2026-06-29 · AI capture assistant — unstructured input → proposed drafts under governance (§10/§2; migration 0030)
Built an AI capture assistant (module 15, `FEATURE_AI_CAPTURE`): paste text / WhatsApp, or upload an image / voice note → a swappable provider EXTRACTS and PROPOSES draft records (client/job/payment/expense) → the human edits/accepts/rejects.

Decisions:
1. **AI proposes; the human Accept is the governance "confirm" (§2).** Extraction writes ONLY `ai_proposal` rows — it **never** creates a domain record. A record is created **only on explicit human Accept**, routed through the EXISTING create service (normal validation, draft state, audit). So nothing AI-proposed is auto-committed — especially money: a payment/expense row exists only after a person clicks Accept.
2. **Provenance marker = `ai_capture_id`** (nullable FK) on party/work_item/payment/expense — null = manual, set = "added by AI". Accept passes it via an optional `opts.aiCaptureId` 4th param on the create services; it is NOT on any create DTO, so it can't be forged on the manual path (`whitelist:true` validation strips unknowns).
3. **Accept can't escalate.** It (a) validates the (edited) `proposed_json` against the real create DTO (plainToInstance + class-validator) and (b) requires the TARGET's own create permission (client→reference:create, job→work:create, payment→billing:create, expense→expenses:create) or System SuperAdmin. Proposal lifecycle pending→accepted|rejected; non-pending can't be re-accepted (double-create guard). Accepted jobs land in work_state='draft'.
4. **Swappable provider** (mirrors EmailService): `AI_CAPTURE_PROVIDER` selects `dev` (default — FREE, zero-cost deterministic heuristic over text; no network/key; declines media with a note), `gemini` (text+image+voice via REST fetch), or `claude` (text+image). Real adapters fail-closed without a key; API keys server-side only; LLM JSON is whitelisted (parseProposals clamps target/confidence) and still re-validated at Accept. No SDK dependency (fetch).
5. **Per-(user, org, day) usage cap** (`AI_CAPTURE_DAILY_CAP`, default 25) via an append-only `ai_usage` ledger, enforced before the provider call (429 when over) with a per-user `pg_advisory_xact_lock` to close the TOCTOU race; a failed extract rolls back so it doesn't consume quota. The free dev adapter is the default so there's a working zero-cost version now; the cap bounds paid-provider spend per tenant.
6. **Media** (image/voice) uploads via the existing file pipeline (file_object + StorageService, ACL-checked); the provider does the vision/transcription step; output is always proposals.

Reviews: security — the four hard rules all hold (no auto-commit, unforgeable provenance, no escalation, money only on human Accept; tenancy/RLS, secrets, append-only intact); fixed the HIGH/MEDIUM cap findings (explicit per-(user,org) scope + advisory-lock). qa — `ai-capture.test.ts` 18/18 (extract commits nothing; Accept creates with `ai_capture_id` + draft; edit/reject; no double-accept; permission gate incl. payment→billing:create; cap 429; DTO validation) + regressions green (DB 353, work 32, billing 21, expense 20). ui — no blockers (no-permission loading guard, consistent labels, Accept disabled until a money proposal has an amount). Needs env: `FEATURE_AI_CAPTURE=true`, `AI_CAPTURE_PROVIDER=dev` (default; gemini/claude + `GEMINI_API_KEY`/`ANTHROPIC_API_KEY` for real), `AI_CAPTURE_DAILY_CAP`.

### 2026-06-29 · Import / Export / Archive (one module, §import; migration 0031)
One module covering bulk import, scoped export, and a dated file archive.

Decisions:
1. **Import routes through the EXISTING create services** (PartyService/WorkService/PaymentService/SettlementService.recordTransfer with an optional `opts.importBatchId`) — never raw inserts — so validation, RLS, audit, canonical reference resolution, and provenance all apply. Every imported record carries an **`import_batch_id`** marker (nullable FK on party/work_item/payment/expense/settlement_transfer; mirrors `ai_capture_id`).
2. **Preview = staged `import_row`, no domain write** (dry-run, like the AI-capture review). Per row: light validation + **read-only** reference (`ReferenceService.lookup`) and party (`PartyService.findByName`) resolution → a "matched / will create" summary. **Commit** validates each row's mapped fields against the REAL create DTO (plainToInstance+validate), requires the entity's own create permission (clients→reference:create, jobs→work:create, payments/settlement→billing:create — no escalation), and creates via the services. **Partial commit:** each row commits in its OWN `withTenant` transaction (a bad row fails alone, the rest succeed) — chosen over savepoints because a nested savepoint dropped the RLS GUC.
3. **Canonical resolution:** university/course/assignment route through ReferenceService (lookup in preview, resolveOrCreate on commit) → "ICT 701" = "ICT701" = "701", no duplicates (verified). Clients/counterparties matched by name; unknown → created (shown in preview), per the confirmed decision.
4. **2025 policy:** the `settlement_opening` entity creates ONLY a dated `settlement_transfer` (via SettlementService.recordTransfer, default date the 2025→2026 boundary, note "2025 opening") — NOT fabricated jobs/legs. The actual 2025 sheet goes in the Archive.
5. **Export reuses the RLS-scoped, permission-gated list read-models** (work.list, payment/invoice list, expense.list, party.search, settlement_transfer under RLS) and serializes them — so an export can never contain a figure the viewer can't see. Each dataset requires its OWN view permission (jobs→work:view, payments/invoices/settlement→billing:view, expenses→expenses:view, clients→reference:view). CSV native; XLSX via **exceljs** (added).
6. **Archive** reuses the file pipeline (FilesService.upload small / link large, kind `archive`) + an `archive_item` metadata table (title/description/doc_date/tags, searchable); a new `archive` branch in the file ACL makes archive files readable by `import_export:view` holders or the uploader. Read-only content.
7. **Templates** (human-friendly headers, the format spec + a sample row) are served by `GET /import/template/:entity` and committed under `/import-helpers/templates`. `/import-helpers/` also holds example Python preprocessors (edit the MAPPING, convert your sheet → the template CSV) + a README documenting the three paths (manual / script / AI capture) + the clients→jobs→payments order + the 2025 policy.
8. Web BFF: dedicated multipart-upload + binary-safe download routes (`/api/import/preview`, `/api/import/template/[entity]`, `/api/export/[dataset]`, `/api/archive/upload`) since the JSON proxy can't carry multipart or binary; JSON ops (commit, archive search) use the generic proxy.

Reviews: security — export-respects-visibility + import-can't-bypass-RLS/validation + canonical dedup + 2025-settlement-only + archive ACL + tenancy all confirmed; fixed L1 (preview now shows the opening date that will be stored). qa — import-export 18/18 (preview writes nothing; commit stamps import_batch_id + canonicalises Monash to one ref_entity; jobs auto-create client + draft; partial commit; settlement→transfer-not-jobs; export 403 without the dataset perm; archive create/search/download) + DB 353/353 + work/billing/expense/settlement/reference regressions green. ui — no blockers (fixed link-only archive rows being un-openable; humanised import status labels). Needs `FEATURE_IMPORT_EXPORT=true`.

### 2026-06-29 · Channels + source-driven routing + N-way profit-share + writer commission (module 17; migration 0032)
Generalised profit-sharing from the hardcoded two-partner pair to N parties at configurable formulas, added admin-creatable source **channels**, and made source the routing key — all as **new rows + one routing concept**, reusing the deal-term/leg/charge/settlement engine. Nothing in the binary settlement (0015) changed (additive).

Decisions:
1. **A channel IS a party (`party_type {channel}`) + a thin `channel` config row** (controller_party_id, medium free-text, is_active). So `work_item.source_party_id` reuses the entire source/leg/deal-term engine for channels with zero new source plumbing; the config row only adds the controller + medium. **Adding/tuning a channel needs no code** (medium is free text, controller is a party FK). RLS = tenant-isolation; it's config (not a ledger) so UPDATE is granted. Historical routing comes from dated deal_terms, **not** from the mutable `controller_party_id` (which only labels the current default residual owner).
2. **One source-routing concept: a new `applies_to` kind `source:<partyId>`** in the shared `resolveDealTerm` (+ a `sourcePartyId` ctx). Scoring: client 4 > **source 3** > jobtype 2 > default 1 (client bumped 3→4 to insert source; existing relative order preserved, no pre-existing term's behaviour changes since every current caller passes no sourcePartyId). A channel is a party, so `source:<channelPartyId>` covers Web/FB **and** `source:<partner/vendor>` uniformly — no separate `channel:` kind.
3. **The doer-conditional channel scheme falls out of the leg chain — no `deal_term` doer dimension.** If Emon does a job he's a node and the existing Momin→Emon `split_pct` applies; if Momin does it Emon isn't a node and gets nothing. The "optional Emon benefit on a business channel" is just a `source:`-scoped profit_share term. So scheme D needed no new routing primitive beyond `source:`.
4. **N-way profit-share = a new `term_type='profit_share'`** reusing the existing `basis` column, `to_party_id = beneficiary`, `from_party_id = NULL` (the business pays). **basis is the configurable FORMULA**: `pct_of_net | pct_after_writer | pct_of_channel | fixed` — set per beneficiary, changeable going forward, effective-dated (a past job keeps its era's term, proven). The pure `deriveProfitShares` resolves EACH beneficiary's most-specific effective term (`resolveProfitShareTerm`, keyed on to_party_id), computes each cut by basis, and the **residual** (pool − Σ cuts) belongs to the business (labelled with the channel's controller for display) — **no explicit House party** (kept the `null = business` convention; adding a 5–10% partner is one term row). Pool, cuts, residual are **derived at read time, never stored**.
5. **Owner dividend = a `default`-scoped profit_share term** — applied structurally by the read-model regardless of whether the owner ever logs in (the protection: a dividend is a DB rule, not a person enforcing it).
6. **§4.4 opacity is the load-bearing risk** (a cut = rate × base ⇒ the beneficiary can back-compute the base). Enforced server-side at TWO points so it survives later edits: (a) **create-term guard** (`setProfitShareTerm`) rejects a percentage `profit_share` to an **active partner** whose base would reveal another partner's private margin — `default`-scoped net/after-writer is always rejected for a partner; a `source:`-scoped percentage is rejected only when the source is **owned by a different partner** (a business-controlled channel benefit, or the partner's own channel, is allowed — that's scheme D); `pct_of_channel` requires a source; **fixed** is always safe. (b) **party-tag guard** (`PartyService.update`) refuses to add `partner` to a party that holds a live default-scoped net dividend (closes the "set term as non-partner, tag partner later" evasion). (c) **read-time**: `my_profit_share` (caller-guarded definer) returns only the caller's own per-job amount + a `scope`; the self-view itemises `scope='source'` rows but exposes default net dividends **aggregate-only** (never per-job), and `profit_share_pool` (the money bases) is reachable only via the `channels:approve`-gated per-job pool endpoint. Confirmed decision: **forbid net-basis for partners** (only fixed / channel-scoped); net basis is for non-partner silent investors, aggregate-only.
7. **Payout = derived + existing ledger** (confirmed): cuts are computed/displayed, never stored; paying one reuses a `settlement_transfer` (off-chain owner) or a business→sharer leg (on-chain). No new payout plumbing.
8. **Writer commission = generalised the platform-fee `charge` mechanism.** `applyPlatformFee` became a private `applyCharge`; `applyWriterCommission` (term_type `writer_commission`, basis `pct|fixed`) produces a party→business `charge` (a % of the writer's job earnings, OR a fixed amount that applies even with zero earnings — the guard is on the COMPUTED amount). Idempotency generalised to `charge_exists(party,job,category)` + a `charge_writer_commission_once` partial unique index. Balance/position needed zero change (it already nets all charge categories).

Reviews: security — no Blocker; the default money/visibility path is correct, append-only, tenant-scoped; the two §4.4 back-computation HIGHs (a partner taking a percentage share of a channel controlled by another partner; the guard evadable by tagging partner later) were both closed (controller-aware source guard + party-tag guard), plus pct_of_channel-requires-source. qa — pure 58 (deriveProfitShares per-basis/N-way/residual/over-alloc + effective-dating; source precedence + client 3→4 re-assert) + channels DB 10 (N-way division via profit_share_pool; my_profit_share opacity — other/non-sharer → 0 rows, no column = the client price; effective-dating non-rewrite; channel tenant isolation; writer_commission charge_exists + unique-index) + settlement HTTP 11 + full DB 383/383, guard:no-stored-profit clean; binary settlement unchanged. ui — no blockers (clean empty/§4.4-hint states, restrained, mobile-first). Needs `FEATURE_CHANNELS=true`.
